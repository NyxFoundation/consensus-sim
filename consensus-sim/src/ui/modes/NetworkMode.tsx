/** Network mode (ネットワークモード) — placeholder until the per-validator
 * at-a-glance grid with hover views is built (planned next). */

export function NetworkMode() {
  return (
    <section className="mode-placeholder">
      <h2>ネットワークモード（準備中）</h2>
      <p>
        各バリデータの状態を一目で確認でき、マウスオーバーでそのバリデータの
        ビューを表示するモードです。次の開発ステップで実装されます。
      </p>
    </section>
  )
}
